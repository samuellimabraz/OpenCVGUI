gradio
fastrtc
opencv-python-headless
mediapipe
numpy

# You might need these depending on your OS/environment
# pytesseract # Add if using OCR functions, needs tesseract-ocr installed system-wide
# Pillow # Usually a dependency of gradio/opencv, but good to list explicitly
